项目适配岗位：
1、Agent 后训练算法设计与实现：主导基模大模型的 Agent 化后训练方案研发，包括指令微调（SFT）、奖励模型（RM）训练、强化学习（RLHF/RLAIF）优化，聚焦任务规划、记忆机制、工具调用、多轮对话一致性等核心能力提升。

2、后训练数据体系构建：设计 Agent 后训练数据的采集、清洗、标注与迭代策略，涵盖通用指令集、任务型对话数据、工具调用样本、多模态交互数据等，建立数据质量评估与筛选机制，优化数据分布与多样性。

3、Agent 能力优化与突破：针对 Agent 在复杂任务拆解、跨领域知识迁移、动态环境适配等场景的痛点，研发创新后训练算法（如分层任务规划训练、多智能体协作训练、反幻觉优化），提升 Agent 的决策准确性与鲁棒性。

4、工程化落地与系统协作：搭建高效后训练流水线，支持大规模数据并行处理、分布式训练（DDP/FSDP）与模型压缩部署；与产品、工程团队协作，打通"后训练-评测-迭代"闭环，适配不同场景下的 Agent 产品需求。

5、技术创新与沉淀：跟踪大模型 Agent 领域前沿技术（如 LLM+Planning、Tool Learning、Multi-Agent Interaction），主导核心技术攻关与专利申请；输出可复用的后训练算法组件、技术文档与评测基准，推动团队技术能力沉淀。

项目：agent_post_training/ — Agent 工具调用能力的完整后训练流水线

主线故事（面试时你可以一句话讲清楚）：

▎ 拿一个基座小模型，通过 数据构造 → SFT → 偏好数据 → DPO → 评测 的完整闭环，把它的工具调用能力从 40% 提升到 80%+，并用同一套 eval harness 量化每一步的收益。

选"工具调用"当载体，因为：贴 Agent、数据可自动合成、评测客观可打分（调用对错能自动判）。

agent_post_training/
├── README.md                     # 主线故事 + 架构图 + 每步收益曲线
├── configs/                      # ← 体现"配置化/工程化"，面试加分
│   ├── sft_lora.yaml
│   ├── dpo.yaml
│   └── fsdp.yaml                 # accelerate FSDP 配置（多卡）
│
├── data/
│   ├── build_sft_data.py         # 合成工具调用 SFT 数据（含 CoT + tool_call 格式）
│   ├── build_pref_data.py        # 用 SFT 模型采样 → 规则/RM 打分 → 造 chosen/rejected
│   └── quality_filter.py         # 数据清洗：去重、格式校验、难度分层（JD第2条）
│
├── train/
│   ├── train_sft.py              # LoRA + accelerate/FSDP，支持多卡
│   ├── train_rm.py               # 奖励模型（Bradley-Terry pairwise）
│   └── train_dpo.py              # DPO（主推，稳定），代码里注释清楚 vs PPO/GRPO 区别
│
├── eval/
│   ├── eval_harness.py           # 工具调用准确率 / 参数正确率 / 多轮一致性
│   ├── benchmark.jsonl           # 自建评测集（JD第5条：评测基准）
│   └── report.py                 # 生成对比报表：base vs SFT vs DPO
│
├── pipeline.py                   # 一键跑通闭环 + 产出收益曲线
└── requirements.txt

每个模块对应 JD 的哪一条（README 里明确标注，面试官一眼看到）：

┌────────────────────────────────┬────────────────────────────────────────┐
│              模块              │                对应 JD                 │
├────────────────────────────────┼────────────────────────────────────────┤
│ train/ SFT+RM+DPO              │ 第 1 条（SFT/RM/RLHF 核心算法）        │
├────────────────────────────────┼────────────────────────────────────────┤
│ data/ 构造+清洗+分层           │ 第 2 条（数据体系）                    │
├────────────────────────────────┼────────────────────────────────────────┤
│ eval + 反幻觉/难度分层注释     │ 第 3 条（能力优化，部分可              │
│                                │ demo，部分写思路）                     │
├────────────────────────────────┼────────────────────────────────────────┤
│ configs/fsdp + accelerate 多卡 │ 第 4 条（分布式 + 流水线闭环）         │
├────────────────────────────────┼────────────────────────────────────────┤
│ benchmark.jsonl + README       │ 第 5 条（评测基准 + 文档）             │
│ 技术沉淀                       │                                        │
└────────────────────────────────┴────────────────────────────────────────┘

技术选型（都用工业界主流，面试可聊）：
- 基座：Qwen2.5-1.5B-Instruct（工具调用能力基础好，1.5B 多卡很快）
- 框架：transformers + peft(LoRA) + trl(DPO/RM) + accelerate(FSDP)
- 我会把 DPO 的 loss 手写一版放在注释里对照 trl 版本，体现你懂原理而不只是调库

---
我建议的推进方式：先搭骨架 + 跑通一条最小闭环（能出收益曲线），再逐步充实。这样你随时有个能跑的东西。

要不要我现在就开始搭？我会先建目录结构和 README + data/build_sft_data.py + eval_harness.py（最小闭环的三块），跑通后再往上加 SFT/DPO 训练。

已建好并本地验证跑通的最小闭环：

agent_post_training/
├── README.md              ✅ 主线故事 + 架构图 + JD 对照表
├── requirements.txt       ✅ base/train 两档依赖
├── data/build_sft_data.py ✅ 合成4类工具调用数据(单工具/多轮/反幻觉/参数缺失)，可复现
├── data/sft_train.jsonl   ✅ 生成的800条训练集
└── eval/
    ├── eval_harness.py     ✅ 4大指标+按类别/难度分桶，支持 mock/pred/model 三种来源
    └── benchmark.jsonl     ✅ 200条held-out评测集

核心价值：现在你已经有一个"能跑出收益曲线"的骨架——哪怕训练脚本还没写，mock 模式就能演示整条评测闭环，面试时可当场 demo。

按依赖顺序，我建议接着做：

1. train/train_sft.py — LoRA + accelerate/FSDP，把 sft_train.jsonl 训进 Qwen2.5-1.5B（GPU 机器跑）
2. data/build_pref_data.py — 用 SFT 模型采样 + eval_harness 的打分逻辑造 chosen/rejected 偏好对
3. train/train_dpo.py — DPO（含手写 loss 对照注释）
4. configs/*.yaml + pipeline.py + eval/report.py — 配置化 + 一键闭环 + 三阶段对比报表

要我继续把**训练侧（1-3）**写出来吗？这些依赖 trl/peft/accelerate，我会写成在 A100 机器上 accelerate launch 就能跑的形式，同时在 Mac 上做语法/逻辑自查。或者你想先自己看看现在这三块、跑一跑再继续？