# 核心课程零依赖：默认的离线 Mock 后端只用 Python 标准库即可运行全部 9 课。
#
# 想接真实模型时，按需安装对应 SDK：
#
#   Anthropic Claude:
anthropic>=0.40.0
#
#   OpenAI / 国产/本地 OpenAI 兼容端点（DeepSeek、通义、Kimi、vLLM、Ollama…）:
openai>=1.40.0
#
# 安装：pip install -r requirements.txt
# 或最小化（仅离线课程）：无需安装任何东西。
