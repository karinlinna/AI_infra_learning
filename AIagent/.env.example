# 复制为 .env 并按需填写。课程默认无需任何配置（走离线 Mock）。

# 选择后端：mock（默认，离线）/ anthropic / openai
LLM_PROVIDER=mock

# --- 使用 Anthropic Claude 时 ---
# LLM_PROVIDER=anthropic
# ANTHROPIC_API_KEY=sk-ant-xxxx

# --- 使用 OpenAI 或"OpenAI 兼容"端点时 ---
# LLM_PROVIDER=openai
# OPENAI_API_KEY=sk-xxxx
# OPENAI_MODEL=gpt-4o-mini
# 指向国产/本地兼容服务时改这里，例如 DeepSeek / 本地 vLLM / Ollama：
# OPENAI_BASE_URL=https://api.deepseek.com/v1
# OPENAI_MODEL=deepseek-chat
